This repository contains model code and unprocessed input data that is not included in citations.
The data files include the NARR atmospheric forcing file and the DIN forcing file from Straub et. al (2015).
